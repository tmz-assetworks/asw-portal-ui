import { InputTooltipDirective } from './input-tooltip.directive';

describe('InputTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
